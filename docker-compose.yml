version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: passport_user
      POSTGRES_PASSWORD: passport_password
      POSTGRES_DB: risk_passport
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
